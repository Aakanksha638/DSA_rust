1) Array: This program computes the minimum number of operations needed to transform an
all-zero array B into a target array A, where each operation increments
(N-1) distinct indices by 1. It returns -1 if no valid sequence of operations
can produce A.